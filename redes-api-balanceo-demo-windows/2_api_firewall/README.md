# Simulación de API de Firewall (Windows o multiplataforma)

Script en Python que simula una llamada a API para configurar una regla de firewall.

## Cómo ejecutar

Instalar `requests`:
```bash
pip install requests
